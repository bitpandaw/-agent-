---
description: RAG Agent 项目上下文与架构约定
alwaysApply: true
---

# 工业设备故障诊断 RAG Agent

## 项目定位
工业设备故障诊断智能问答系统，RAG + 基于 Function Calling 的多步工具调用循环（orchestrator 控制 max_steps）+ 多工具（search_knowledge / query_qa_records / search_article_graph / calculator）。技术栈：ChromaDB、SentenceTransformer、DeepSeek、FastAPI、Neo4j，自研架构（未用 LangChain）。**HotpotQA 仅用于测试与验证**，实际场景可切换为工业知识库。

## 核心模块
| 模块 | 职责 |
|------|------|
| gateway | FastAPI 入口，/chat、SessionStore |
| orchestrator | 基于Function Calling的多步工具调用循环，调 planner + executor，控制 max_steps |
| planner | 调 LLM，解析 tool_calls |
| executor | 执行 TOOL_REGISTRY 中的工具 |
| tools | search_knowledge（→ RAG HTTP）、calculator、query_qa_records、search_article_graph |
| rag | 分块、索引、检索，min-max 归一化 |

## 数据契约
- **PlanAction**: `{tool_name, tool_args, tool_call_id}`
- **ToolResult**: `{ok, code, message, payload, latency_ms, tool_name}`

## 分布式改造顺序（见开发记录/DISTRIBUTED_ARCHITECTURE.md）
1. Gateway + Redis  2. Planner 独立  3. RAG 已独立  4. Executor 独立  5. Orchestrator 独立  6. Docker Compose
